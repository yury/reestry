<select name="district" class="required">
  <% if is_search %>
    <option value="">Не имеет значения</option>
  <% end %>
  <%=  options_from_collection_for_select(District.find_all_by_location_id(location_id), :id, :name, params[:district].to_i)  %>
</select>