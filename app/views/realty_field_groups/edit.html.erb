<h1>Editing realty_field_group</h1>

<% form_for(@realty_field_group) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @realty_field_group %> |
<%= link_to 'Back', realty_field_group_path %>
